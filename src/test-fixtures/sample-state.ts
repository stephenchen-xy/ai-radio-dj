import type { StorylineState } from '../domain/storyline';

export const sampleStorylineState: StorylineState = {
  mainlineLabel: 'late-focus',
  currentSegmentLabel: 'late-focus',
  dominantDriver: 'mixed',
  confidence: 0.7
};
